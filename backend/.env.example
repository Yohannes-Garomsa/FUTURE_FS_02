# Django Settings
# NOTE: Use DEBUG=True only for local development.
DEBUG=False
SECRET_KEY=CHANGE_ME_TO_A_STRONG_RANDOM_VALUE
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
DB_NAME=crm_db
DB_USER=crm_user
DB_PASSWORD=CHANGE_ME
DB_HOST=localhost
DB_PORT=5432

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME=15 minutes
JWT_REFRESH_TOKEN_LIFETIME=1 day
